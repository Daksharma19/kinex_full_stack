# backend

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

This project was created using `bun init` in bun v1.3.14. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.

## Docs

- [API_ROUTES.md](./API_ROUTES.md) — endpoint reference and auth model.
- [TESTING.md](./TESTING.md) — how to test the Supabase signup → confirm → login → profile flow in Postman.
